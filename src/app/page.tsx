// src/app/page.tsx
// This is a placeholder root page.
// The middleware should redirect users to their appropriate language route (e.g., /en, /zh).
// This page might briefly render before the redirect or if middleware fails.

export default function RootPage() {
  // Return null or a minimal loading indicator, 
  // as the middleware is expected to handle the redirect.
  return null;
  // Or return <p>Loading...</p>; 
} 